# arachne-panel

Check out the [main repo](https://github.com/lein3000zzz/project-arachne)

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run start
```

Accessible via `localhost:3003`.

Currently only sends runs to kafka. More to come!